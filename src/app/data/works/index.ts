import { eventImages } from "./events";
import { productsImages } from "./products";
import { portraitsImages } from "./portraits";
import { sportImages } from "./sports";

export const imageCollections: { [key: string]: any[] } = {
  events: eventImages,
  products: productsImages,
  portraits: portraitsImages,
  sport: sportImages,
};
